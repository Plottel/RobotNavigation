To run the program, navigate to:

RobotNavigation/bin/Windows/x86/Debug/RobotNavigation.exe

The .exe can be run to execute the visualizer. You can also open a terminal and run the program with command line arguments as per the report.

The name of the provided test file is also in the Debug folder called "RobotNav-test.txt" 